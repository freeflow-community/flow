# Slack connector: deployable on Railway

- [server] Connector honours `HOST` (default loopback) and `CONNECTOR_LOCK=none` for hosts that already guarantee one container per volume. Railway service settings and recipe in `docs/dev/SLACK_CONNECTOR.md`.
