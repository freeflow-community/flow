# iOS: bump build number to 2.0 (18)

- `[ios]` For the TestFlight upload carrying the first-paint fix (#200) on
  top of the thread-nav fix (#205); App Store Connect rejects a re-used
  build number.
