# Drop the duplicate workspace avatar from the web sidebar header

- `[web]` The sidebar header now shows just the workspace name and its
  dropdown caret — the avatar it repeated is already in the left workspace
  rail, which stays the only place it is drawn (#422).

## Feature

- **A tidier sidebar header on web.** The workspace avatar no longer appears
  twice: it shows once in the left workspace rail, and the sidebar header is
  just the workspace name.
