# macOS side panel keeps the sidebar visible

- `[macos]` Keep the workspace rail and channel sidebar anchored when the right-side panel opens.

## Feature

- **Stable three-column layout on Mac.** Opening a thread or file panel no longer pushes the left sidebar off-screen.
