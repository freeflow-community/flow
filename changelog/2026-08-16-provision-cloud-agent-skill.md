# Skill: provision a cloud-running Flow agent

- `[bridge]` New `.claude/skills/provision-cloud-agent` skill: provisions an
  always-on Flow agent on Railway (devcontainer image, persistent volume,
  bridge daemon as non-root main process), with the CLI/permission traps
  documented from the first RW1 setup.
