# iOS marketing version → 2.1

- [ios] `MARKETING_VERSION` 2.0 → 2.1 (app + share extension) — 2.0 is now
  published in the App Store, so the next submission needs a new version.
  Build numbers stay derived by `release-ios.sh`; nothing else changes.
