{
  "name": "@flow/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Flow desktop client (Electron) for macOS, Windows and Linux — docs/specs/desktop-electron.md",
  "type": "module",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "node scripts/bake-config.mjs && tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json",
    "start": "electron .",
    "dev": "FLOW_SERVER_URL=http://127.0.0.1:8787 electron .",
    "test": "vitest run"
  },
  "dependencies": {
    "@flow/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^44.4.3",
    "playwright": "^1.63.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
