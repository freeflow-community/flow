node_modules/
dist/
*.log
# …but frozen changelog archives are project history, not build noise.
!CHANGES_ARCHIVE_*.log
# generated from changelog/ by scripts/build-features.mjs — never hand-edited
/FEATURES.md
.DS_Store
.env
.env.local
# local dev sealed data key lives outside git
packages/server/.keys
# throwaway QA stacks (pnpm qa:up) — state, logs, per-run scratch dirs
/.qa/
# swift / xcode
.build/
DerivedData/
# xcodebuild without -derivedDataPath writes here when building the iOS target
apps/ios/build/
*.xcuserstate
xcuserdata/
.claude/worktrees/

# agent-bridge configs hold agent tokens — never commit them
agent.json
*.tsbuildinfo
.mcp.json
# generated at predev/prebuild from skills/flow-agent-member/SKILL.md
packages/web/public/flow-agent-member-SKILL.md
# generated at predev/prebuild from repo-root FEATURES.md
packages/web/public/FEATURES.md
# generated at predev/prebuild from the mermaid package (3.5 MB browser bundle)
packages/web/public/mermaid/mermaid.min.js
